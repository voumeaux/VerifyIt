A simple, fast Java tool that keeps an eye on your files. It works by creating a (SHA-256 hash) for each file you’re monitoring. Whenever you check a file, it recalculates the hash and compares it to the one stored earlier. It alerts you when the file has been changed
